<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trip Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #555545;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            width: 50%;
            background-color: #ccccc4;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px #97704f ;
        }

        h1 {
            text-align: center;
            color: #97704f;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            margin-bottom: 10px;
            font-weight: bold;
            color: #97704f;
        }

        input[type="text"] {
            width: 100%;
            padding: 8px;
            margin-bottom: 15px;
            border: 1px solid #97704f;
            border-radius: 4px;
            box-sizing: border-box;
            
        }

        button[type="submit"] {
            background-color: #555545;
            color: #0a0a0a;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }

        button[type="submit"]:hover {
            background-color: #555545;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Trip Details</h1>
        <form action="/get-trip-details" method="POST">
            <label for="to">Enter To Address:</label>
            <input type="text" id="to" name="to" required>
            <label for="fro">Enter From Address:</label>
            <input type="text" id="fro" name="fro" required>

            <button type="submit">Search</button>
        </form>
    </div>
</body>
</html>
