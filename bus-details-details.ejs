<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bus Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #555545;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            width: 50%;
            background-color: #ccccc4;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px #97704f;
        }

        h1 {
            text-align: center;
            color: #97704f;
        }

        .details {
            margin-top: 20px;
        }

        .details p {
            margin-bottom: 10px;
            color: #555;
        }

        .error {
            color: red;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Bus Details</h1>
        <% if (busDetails) { %>
            <div class="details">
                <p><strong>Bus ID:</strong> <%= busDetails.busId %></p>
                <p><strong>Depo:</strong> <%= busDetails.depo %></p>
                <p><strong>Number of Seats:</strong> <%= busDetails.noOfSeats %></p>
                <p><strong>Driver Name:</strong> <%= busDetails.drivername %></p>
                <p><strong>Owner Name:</strong> <%= busDetails.ownername %></p>
            </div>
        <% } else { %>
            <p class="error">Bus not found</p>
        <% } %>
    </div>
</body>
</html>
